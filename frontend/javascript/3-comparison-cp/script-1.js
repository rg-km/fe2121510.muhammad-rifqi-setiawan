// Pengecekan angka positif, negatif, atau 0 dengan nested if

// Masukan suatu angka
const number = 5;

// TODO: answer here
if (number >= 0) {
    if (number == 0) {
      console.log('merupakan angka 0');
    } else {
      console.log('merupakan angka positif');
    }
} else {
console.log('merupakan angka negatif');
}
